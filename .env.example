# Docker host (optional - defaults to local Docker socket)
# DOCKER_HOST=tcp://192.168.1.100:2376

# Docker TLS verify (optional - for remote Docker with TLS)
# DOCKER_TLS_VERIFY=1

# Docker cert path (optional - for remote Docker with TLS)
# DOCKER_CERT_PATH=~/.docker/certs
